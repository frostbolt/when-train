# Copy to `.env` on the deployment host. docker compose reads it automatically.

# The public hostname Caddy serves. Setting this to a real domain makes Caddy
# obtain and renew a Let's Encrypt certificate automatically (HTTPS on :443,
# HTTP→HTTPS redirect on :80). Leave unset / ":80" for plain HTTP locally.
SITE_ADDRESS=whentrain.lushchik.com
